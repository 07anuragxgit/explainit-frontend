# ExplainIt Frontend

Vite React frontend for ExplainIt.

## Setup Locally

1. Install dependencies: `npm install`
2. Create `.env` from `.env.example` (default backend connects to http://localhost:8000)
3. Run locally: `npm run dev`

> **WARNING**: Never push your `.env` file to GitHub!
